# Melos Starter Project

This is a starter project for Melos, a tool for managing mono-repos with mixed package types.

## Usage

`dart pub run melos exec "FLUTTER_COMMAND"`

for example:

`dart pub run melos exec "flutter pub get"`
